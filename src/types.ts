export type Circuit = {
  name: string;
  image: string;
};

export type Settings = {
  peakVoltage: number;
  pulsation: number;
  phase: number;
  resistence: number;
  capacity: number;
  inductance: number;
};
